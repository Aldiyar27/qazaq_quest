@{
    ViewData["Title"] = "Регистрация";
}

<<<<<<< HEAD
<style>

*{box-sizing:border-box}
body{
  margin:0;
  min-height:100vh;
  display:flex;
  justify-content:center;
  align-items:center;
  background:linear-gradient(135deg,#3d8b6e,#f6b483);
  font-family:'Archivo',sans-serif;
}

.card{
  background:#fff;
  padding:3.5rem 3rem;
  border-radius:2.5rem;
  max-width:440px;
  width:100%;
  text-align:center;
  box-shadow:0 25px 50px rgba(0,0,0,.2);
}

h1{
  font-family:'Clash Display',sans-serif;
  font-size:2.6rem;
  margin-bottom:.5rem;
  color:#2b5e4b;
}

p{color:#9c6a46;margin-bottom:2rem}

input{
  width:100%;
  padding:1rem 1.2rem;
  border-radius:1rem;
  border:1px solid #f0c8a8;
  margin-bottom:1.1rem;
  font-size:1rem;
}

button{
  width:100%;
  padding:1rem;
  border:none;
  border-radius:2rem;
  background:#286b53;
  color:#fff;
  font-size:1.1rem;
  font-weight:600;
  cursor:pointer;
  box-shadow:0 15px 25px rgba(40,107,83,.35);
}

a{
  display:inline-block;
  margin-top:1.5rem;
  color:#2b5e4b;
  text-decoration:none;
  font-weight:600;
}
</style>

<form method="post" action="/Auth/Register">
    <div class="card">
        <h1>Регистрация</h1>
        <p>Создай аккаунт и начни приключение</p>

        <input type="text" name="name" placeholder="Имя" required>
        <input type="email" name="email" placeholder="Email" required>
        <input type="password" name="password" placeholder="Пароль" required>
        <input type="password" name="confirmPassword" placeholder="Повтори пароль" required>

        <button type="submit">Создать аккаунт</button>

        <a href="/Auth/Login">Уже есть аккаунт? Войти</a>
    </div>
</form>
=======
<section class="auth-section">
    <div class="auth-card panel auth-card-rich">
        <div class="eyebrow">Создай аккаунт</div>
        <h1>Регистрация</h1>
        <p class="muted">Без регистрации нельзя участвовать в квестах. Имя и email должны быть уникальными, а пароль — безопасным.</p>

        <form method="post" asp-controller="Auth" asp-action="Register" class="auth-form">
            <div class="field">
                <label>Имя пользователя</label>
                <input type="text" name="name" placeholder="Aldiyar_01" minlength="3" maxlength="30" required>
                <small class="field-hint">Нельзя зарегистрироваться под уже существующим именем.</small>
            </div>

            <div class="field">
                <label>Email</label>
                <input type="email" name="email" placeholder="you@example.com" required>
                <small class="field-hint">Один email можно использовать только для одного аккаунта.</small>
            </div>

            <div class="field">
                <label>Пароль</label>
                <input type="password" name="password" placeholder="Минимум 8 символов" required>
                <small class="field-hint">8–20 символов, заглавная и строчная буква, цифра, спецсимвол. Без пробелов, без abc/123 и без трёх одинаковых символов подряд.</small>
            </div>

            <div class="field">
                <label>Подтверждение пароля</label>
                <input type="password" name="confirmPassword" placeholder="Повтори пароль" required>
            </div>

            <button class="btn btn-primary auth-btn" type="submit">Создать аккаунт</button>
        </form>

        <div class="auth-links">
            <a asp-controller="Auth" asp-action="Login">Уже есть аккаунт? Войти</a>
            <a asp-controller="Home" asp-action="Index">Вернуться на главную</a>
        </div>
    </div>
</section>
>>>>>>> d34208a (feature 2.0)
